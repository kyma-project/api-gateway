## Bug Fixes

- The ExternalGateway resource is now created only after the accompanying EnvoyFilters are in place. This prevents a race in which the Gateway reported `Ready` while the filters that protect the workloads were still missing, briefly exposing traffic without the intended enforcement. See [#2735](https://github.com/kyma-project/api-gateway/pull/2735).
