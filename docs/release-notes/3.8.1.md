## Bug Fixes

- The ExternalGateway resource is now created only after the accompanying EnvoyFilters are in place. This ensures the gateway does not report the `Ready` state while the filters that protect the workloads are still missing, preventing a brief period during which traffic could be exposed without the intended enforcement. See [#2735](https://github.com/kyma-project/api-gateway/pull/2735).
