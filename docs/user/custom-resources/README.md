# Custom Resources <!-- {docsify-ignore-all} -->

## APIGateway Custom Resource

The `apigateways.operator.kyma-project.io` CustomResourceDefinition (CRD) describes the kind and the format of data that APIGateway Controller uses to configure resources. See [APIGateway Custom Resource](./apigateway/04-00-apigateway-custom-resource.md).

## APIRule Custom Resource

The `apirules.gateway.kyma-project.io` CRD describes the kind and the format of data the APIRule Controller uses to configure resources. See [APIRule Custom Resource](./apirule/04-10-apirule-custom-resource.md).

## RateLimit Custom Resource

The `ratelimits.gateway.kyma-project.io` CRD describes the kind and the format of data that RateLimit Controller uses to configure the request rate limits for applications. See [RateLimit Custom Resource](./ratelimit/04-10-ratelimit-custom-resource.md).