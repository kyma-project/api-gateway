---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: externalgateways.gateway.kyma-project.io
spec:
  group: gateway.kyma-project.io
  names:
    categories:
    - all
    - kyma-project
    - gateway
    kind: ExternalGateway
    listKind: ExternalGatewayList
    plural: externalgateways
    singular: externalgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gateway
      name: Gateway
      type: string
    - jsonPath: .spec.externalDomain
      name: External Domain
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalGateway is the Schema for the externalgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalGatewaySpec defines the desired state of ExternalGateway
            properties:
              caSecretRef:
                description: |-
                  CASecretRef references the Secret containing the CA certificate
                  This CA is used to validate client certificates during mTLS handshake
                  If namespace is not specified, defaults to the ExternalGateway's namespace
                  The Secret must contain a 'cacert' key (Istio convention)
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              externalDomain:
                description: ExternalDomain is the customer-facing domain (e.g., api.customer.com)
                maxLength: 255
                pattern: ^(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$
                type: string
              gateway:
                description: Gateway is the name of the Istio Gateway to be created
                  in the application namespace
                maxLength: 63
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              internalDomain:
                description: InternalDomain configuration for Kyma-internal access
                properties:
                  kymaSubdomain:
                    description: |-
                      KymaSubdomain is the subdomain prefix (e.g., "external-myapp")
                      The full internal domain will be: {kymaSubdomain}.{KYMA_DOMAIN}
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - kymaSubdomain
                type: object
              regions:
                description: |-
                  Regions is a list of UGW region identifiers (e.g., "aws/eu-central-1")
                  These must match regions defined in the RegionsConfigMapRef ConfigMap
                  Only the first region in the list will be used
                items:
                  type: string
                minItems: 1
                type: array
              regionsConfigMapRef:
                description: |-
                  RegionsConfigMapRef references the ConfigMap containing region certificate subjects
                  The ConfigMap must contain a 'regions.yaml' key with region metadata
                  If namespace is not specified, defaults to the ExternalGateway's namespace
                properties:
                  key:
                    description: The key to select.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the ConfigMap or its key must be
                      defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
            required:
            - caSecretRef
            - externalDomain
            - gateway
            - internalDomain
            - regions
            - regionsConfigMapRef
            type: object
          status:
            description: ExternalGatewayStatus defines the observed state of ExternalGateway
            properties:
              description:
                description: Contains the description of the ExternalGateway's status
                type: string
              lastProcessedTime:
                description: Represents the last time the ExternalGateway status was
                  processed
                format: date-time
                type: string
              state:
                description: Defines the reconciliation state of the ExternalGateway
                enum:
                - Processing
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
