apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  hosts:
    - "{{.Host}}"
  gateways:
    - "{{.Gateway}}"
  http:
    - route:
        - destination:
            host: "{{.DestinationHost}}"
