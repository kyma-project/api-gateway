apiVersion: v1
kind: Namespace
metadata:
  name: "{{.Namespace}}"
  labels:
    istio-injection: enabled
---

apiVersion: operator.kyma-project.io/v1alpha1
kind: APIGateway
metadata:
  name: default
spec:
  enableKymaGateway: true
