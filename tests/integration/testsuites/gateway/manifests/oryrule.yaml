apiVersion: oathkeeper.ory.sh/v1alpha1
kind: Rule
metadata:
  name: "{{.Name}}"
  namespace: "{{.Namespace}}"
spec:
  match:
    methods:
      - GET
    url: >-
      <http|https>://example.com</.*>
